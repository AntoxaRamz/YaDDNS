apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'application'

mainClassName = 'yaddns.Application'

repositories {
    mavenCentral()
}

dependencies {
        compile 'org.json:json:20150729'
}

jar {
    baseName = 'ddns'
       
    manifest {
        attributes(
            'Main-Class': 'yaddns.Application'
        )
    }
 
   from configurations.compile.collect {
        it.isDirectory() ? it : zipTree(it)
   }
}